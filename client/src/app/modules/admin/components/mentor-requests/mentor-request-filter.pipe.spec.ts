import { MentorRequestFilterPipe } from './mentor-request-filter.pipe';

describe('MentorRequestFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MentorRequestFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
